#include<bits/stdc++.h>
using namespace std;

int main(){
    vector<bool> is_prime(1e8+2,true);
    vector<int>primes;

    for(int i = 2 ; i < v.size();i++)
    {
        if( is_prime[i] ){primes.push-back(i)};
        for (int j = i; j < count; i++)
        {
            /* code */
        }
        
    }
    return 0;
}