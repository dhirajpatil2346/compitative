#include <bits/stdc++.h>
using namespace std;
using LL = long long;
void solve()
{
}
int main()
{
    vector<LL> v;
    LL x;
    while (true)
    {
        cin >> x;
        if (x == -1)
            break;
        v.push_back(x);
    }
    return 0;
}