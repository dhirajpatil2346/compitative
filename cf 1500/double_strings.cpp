#include <bits/stdc++.h>
using namespace std;
#define LL long long
#define endl "\n"
void solve()
{
    int n;
    cin >> n;
    map<int, vector<string>> m;
    vector<string> v;
    for (int i = 0; i < n; i++)
    {
        cin >> v[i];
        m[v[i].length()].push_back(v[i]);
    }
    
}
int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    int t;
    cin >> t;
    while (t--)
    {
    }
    return 0;
}